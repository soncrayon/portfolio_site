showDetails = (parentElement) => {
    // find the element with the corresponding details classname and remove 'hidden' from visibility

}

hideDetails = (parentElement) => {
    // toggle the visibility of the corresponding details classname 
}